<section class="wholesale wholesale__logos">
  <div class="logos__inner gutter speks-slider speks-slider__lime">
    <div class="logos__logo">
      {% if section.settings.image_1 != blank %}
        {% include 'responsive-image' with
          image: section.settings.image_1,
          max_width: 300,
          max_height: 300
        %}
      {% endif %}
    </div>
    <div class="logos__logo">
      {% if section.settings.image_2 != blank %}
        {% include 'responsive-image' with
          image: section.settings.image_2,
          max_width: 300,
          max_height: 300
        %}
        {% endif %}
    </div>
    <div class="logos__logo">
      {% if section.settings.image_3 != blank %}
        {% include 'responsive-image' with
          image: section.settings.image_3,
          max_width: 300,
          max_height: 300
        %}
        {% endif %}
    </div>
    <div class="logos__logo">
      {% if section.settings.image_4 != blank %}
        {% include 'responsive-image' with
          image: section.settings.image_4,
          max_width: 300,
          max_height: 300
        %}
        {% endif %}
    </div>
    <div class="logos__logo">
      {% if section.settings.image_5 != blank %}
        {% include 'responsive-image' with
          image: section.settings.image_5,
          max_width: 300,
          max_height: 300
        %}
        {% endif %}
    </div>
  </div>
</section>


{% schema %}
  {
    "name": "Wholesale Logos",
    "settings": [
      {
        "type": "image_picker",
        "id": "image_1",
        "label": "Logo 1"
      },
      {
        "type": "image_picker",
        "id": "image_2",
        "label": "Logo 2"
      },
      {
        "type": "image_picker",
        "id": "image_3",
        "label": "Logo 3"
      },
      {
        "type": "image_picker",
        "id": "image_4",
        "label": "Logo 4"
      },

      {
        "type": "image_picker",
        "id": "image_5",
        "label": "Logo 5"
      }

    ],
    "presets": [
      {
        "name": "Wholesale Logos",
        "category": "Image"
      }
    ]
  }
{% endschema %}
